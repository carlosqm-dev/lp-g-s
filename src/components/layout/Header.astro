---
// Header fijo, transparente sobre el Hero. Pasa a fondo blanco + logo/enlaces
// oscuros al hacer scroll — el toggle de la clase `.is-scrolled` vive en
// src/animations/header-scroll.ts (GSAP ScrollTrigger).
//
// Mismo grid de 3 columnas en los dos breakpoints (izquierda / centro /
// derecha); solo cambia quien ocupa cada celda:
//   escritorio -> nav        | logo horizontal | CTA
//   mobile     -> hamburguesa| isotipo         | CTA
// Los accesos de mobile caen en una tarjeta flotante bajo el header.
// Comportamiento del menu: src/animations/header-menu.ts.
import { contact } from '../../data/contact';
import { navLinks } from '../../data/nav';
---
<header
  id="site-header"
  class="fixed inset-x-0 top-0 z-50 border-b border-transparent transition-colors duration-300"
>
  <div class="mx-auto grid max-w-6xl grid-cols-[1fr_auto_1fr] items-center px-6 py-4 md:px-16">
    <button
      id="menu-toggle"
      type="button"
      class="menu-toggle -ml-2 flex h-10 w-10 items-center justify-center justify-self-start md:hidden"
      aria-label="Abrir menú"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="menu-toggle-bars relative block h-3.5 w-6" aria-hidden="true">
        <span class="menu-bar menu-bar-top"></span>
        <span class="menu-bar menu-bar-mid"></span>
        <span class="menu-bar menu-bar-bottom"></span>
      </span>
    </button>

    <nav class="header-nav hidden items-center gap-6 justify-self-start md:flex" aria-label="Principal">
      {navLinks.filter((link) => link.primary).map((link) => (
        <a href={link.href} class="header-nav-link relative text-sm font-bold transition-colors duration-300">
          {link.label}
        </a>
      ))}
    </nav>

    <a href="#hero" class="relative block h-11 w-11 justify-self-center md:hidden" aria-label="G&S Conexiones Estratégicas — inicio">
      <img
        src="/brand/isotipo-white.png"
        alt=""
        aria-hidden="true"
        class="header-logo-white absolute inset-0 h-full w-full object-contain transition-opacity duration-300"
      />
      <img
        src="/brand/isotipo-black.png"
        alt=""
        aria-hidden="true"
        class="header-logo-black absolute inset-0 h-full w-full object-contain opacity-0 transition-opacity duration-300"
      />
    </a>

    <a href="#hero" class="relative hidden aspect-[640/312] justify-self-center md:block md:h-14">
      <img
        src="/brand/logo-horizontal-white.png"
        alt="G&S Conexiones Estratégicas"
        class="header-logo-white absolute inset-0 h-full w-full object-contain transition-opacity duration-300"
      />
      <img
        src="/brand/logo-horizontal-black.png"
        alt=""
        aria-hidden="true"
        class="header-logo-black absolute inset-0 h-full w-full object-contain opacity-0 transition-opacity duration-300"
      />
    </a>

    <a
      href={contact.whatsappHref}
      target="_blank"
      rel="noopener noreferrer"
      class="justify-self-end rounded-sm bg-accent px-4 py-2 text-sm font-bold text-primary transition-base hover:opacity-90"
    >
      Contacto
    </a>
  </div>

  <div
    id="mobile-menu"
    class="mobile-menu absolute inset-x-4 top-full flex flex-col rounded-3xl border px-6 py-3 md:hidden"
  >
    <nav class="flex flex-col" aria-label="Menú móvil">
      {navLinks.map((link) => (
        <a href={link.href} class="mobile-menu-link py-4 text-xl text-background/90 transition-colors duration-200">
          {link.label}
        </a>
      ))}
    </nav>
  </div>
</header>

<style>
  .header-nav a {
    color: var(--color-background);
  }

  .header-nav-link::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: -4px;
    height: 2px;
    background-color: currentColor;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s var(--ease-standard);
  }

  .header-nav-link:hover::after {
    transform: scaleX(1);
  }

  #site-header.is-scrolled {
    background-color: var(--color-background);
    border-color: var(--color-background-alt);
  }

  #site-header.is-scrolled .header-nav a {
    color: var(--color-secondary);
  }

  #site-header.is-scrolled .header-logo-white {
    opacity: 0;
  }

  #site-header.is-scrolled .header-logo-black {
    opacity: 1;
  }

  /* Isotipo y hamburguesa van sueltos sobre el header, sin contenedor
     propio: heredan el mismo crossfade blanco -> negro que el resto. */
  .menu-toggle {
    color: var(--color-background);
  }

  #site-header.is-scrolled .menu-toggle {
    color: var(--color-secondary);
  }

  /* --- Boton hamburguesa: 3 barras que se cruzan en X al abrir --- */
  .menu-bar {
    position: absolute;
    left: 0;
    height: 2px;
    width: 100%;
    background-color: currentColor;
    transition:
      transform 0.3s var(--ease-standard),
      opacity 0.2s var(--ease-standard);
  }

  .menu-bar-top {
    top: 0;
  }

  .menu-bar-mid {
    top: 50%;
    transform: translateY(-50%);
  }

  .menu-bar-bottom {
    bottom: 0;
  }

  #site-header.menu-open .menu-bar-top {
    transform: translateY(6px) rotate(45deg);
  }

  #site-header.menu-open .menu-bar-mid {
    opacity: 0;
  }

  #site-header.menu-open .menu-bar-bottom {
    transform: translateY(-6px) rotate(-45deg);
  }

  /* --- Tarjeta flotante de navegacion (mobile), navy en liquid glass ---
     El navy translucido deja pasar el fondo; el blur + saturate lo funden
     y el borde/highlight superior simulan el canto de vidrio. */
  .mobile-menu {
    border-color: color-mix(in srgb, var(--color-background) 22%, transparent);
    background-color: color-mix(in srgb, var(--color-primary) 55%, transparent);
    -webkit-backdrop-filter: blur(24px) saturate(180%);
    backdrop-filter: blur(24px) saturate(180%);
    box-shadow:
      0 24px 48px -12px color-mix(in srgb, var(--color-primary) 45%, transparent),
      inset 0 1px 0 color-mix(in srgb, var(--color-background) 28%, transparent);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-0.5rem);
    transition:
      opacity 0.3s var(--ease-standard),
      transform 0.3s var(--ease-standard),
      visibility 0.3s;
  }

  #site-header.menu-open .mobile-menu {
    opacity: 1;
    visibility: visible;
    transform: none;
  }

  .mobile-menu-link:active {
    color: var(--color-accent);
  }
</style>
